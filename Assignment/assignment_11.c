#include <stdio.h>
#include <math.h>

int main()
{
    int num, i;
    double sqrt_result;
    long long factorial = 1;

    printf("Enter a number: ");
    scanf("%d", &num);

    
    sqrt_result = sqrt(num);

    int square = num * num;

    int cube = num * num * num;

    
}
